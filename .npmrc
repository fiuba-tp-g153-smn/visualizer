# Pin exact versions in package.json instead of ranges (reproducible installs)
save-exact=true

# Fail if the active Node/npm don't satisfy package.json "engines"
engine-strict=true

# Quiet noisy CLI output
fund=false
audit=false
