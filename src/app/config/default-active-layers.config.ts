/**
 * Lista de IDs de capas que deben estar activas por defecto
 * si el usuario no tiene una configuración guardada
 */
export const DEFAULT_ACTIVE_LAYERS: string[] = ['ign-limite-internacional', 'ign-provincia'];
